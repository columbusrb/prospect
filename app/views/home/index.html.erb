<div class="container">
  <div class="row">
    <div class="col m12 xl8">
      <%= image_tag("crb_prospect_scatter.png", alt: "Columbus Ruby Brigade: Prospect") %>
      <h1>Welcome!</h1>

        <p>
          We dig <span class="ruby">Ruby</span> and we dig elections! This is the voting module for <a href="https://columbusrb.com" target="_blank">Columbus Ruby Brigade.</a><br>
          You can register as a candidate, view candidates' posted information, and vote (when it's open).
        </p>
        <% unless signed_in? %>
          <p>
            Log in with your Slack account. You need to join CRB's <a href="https://crb-slack-invite.herokuapp.com/" target="_blank">Slack</a> in order to run as a candidate and vote.
          </p>
        <% end %>
      <% unless voting_over? %>
        <p>
          <span class="rainbow-text">Nominations Run:</span> <br>
          <span><%= l nominations_starts %> - <br> 
          <%= l nominations_ends %></span>
        </p>
        <p>
          <span class="rainbow-text">Voting Runs:</span> <br>
          <span><%= l voting_starts %> - <br>
          <%= l voting_ends %></span>
        </p>
      <% end %>
      <% if voting_over? %>
          <p>
            <span class="rainbow-text">Voting is over! Go see the election results now!</span>
          </p>
      <% end %>
    </div>
  </div>
</div>